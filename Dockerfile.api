FROM oven/bun:1.2.15-alpine

WORKDIR /app

# Install only backend dependencies first for layer caching.
COPY package.json bun.lock ./
RUN bun install --frozen-lockfile

# Copy backend sources and runtime files.
COPY src ./src
COPY tsconfig.json ./

EXPOSE 8000

CMD ["bun", "run", "start"]
